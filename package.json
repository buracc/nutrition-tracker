{
    "name": "nutrition-tracker",
    "version": "1.0.0",
    "description": "A simple nutrition tracking app",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "build": "mkdir build && cp public/* build/",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d build"
    },
    "dependencies": {
        "express": "^4.17.1"
    },
    "devDependencies": {
        "gh-pages": "^6.1.1"
    }
}